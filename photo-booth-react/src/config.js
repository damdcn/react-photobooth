const config = {
    time: {
        delay: 5, // countdown for the photo to be taken
        timeout: 30 // time during which the preview will be visible
    },
    API: {
        local: 'http://localhost:5000',
        dev: 'http://localhost:5000'
    }
}

export default config;